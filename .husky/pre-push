#!/usr/bin/env sh

echo "🔍 Running TypeScript type check before push..."

npx tsc --noEmit

if [ $? -ne 0 ]; then
  echo ""
  echo "❌ Blocked: TypeScript errors found. Fix all type errors before pushing."
  echo ""
  exit 1
fi

echo "✅ TypeScript check passed. Pushing..."
